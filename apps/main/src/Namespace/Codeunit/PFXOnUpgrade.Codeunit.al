codeunit 50101 "PREFIX OnUpgrade"
// An install codeunit includes AL methods for performing operations unconcerned with 
// the extension code itself during the initial installation and the reinstallation of an extension.
{
    Subtype = Upgrade;

    trigger OnUpgradePerCompany()
    begin

    end;
}